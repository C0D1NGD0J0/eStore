import React from 'react';

const Pagination = () => {
  return (
    <div className="pagination">
      <button className="btn btn-pagination btn-lg">View More</button>
    </div>
  );
}

export default Pagination;
